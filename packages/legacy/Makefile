.PHONY: test

# Legacy package: read-only reference. No install target.
# Tests run against the legacy SQL but failures do not block core CI.

test:
	@echo "Legacy tests: informational only. See packages/legacy/FROZEN.md."
	@echo "No tests to run in legacy package."
